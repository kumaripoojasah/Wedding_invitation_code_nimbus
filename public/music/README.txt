The homepage currently uses the Engagement MP3 configured in src/App.tsx.

To use a local licensed track instead, add it here as invitation.mp3 and
change the audio source in src/App.tsx to /music/invitation.mp3.

Do not add the audio file to src/lib/utils.ts.
